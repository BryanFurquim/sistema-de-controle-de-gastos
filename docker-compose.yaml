services:
  app:
   build: .
   ports:
    - "8082:80"
   depends_on:
    - db  

  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: cadastro_de_gastos
      MYSQL_ROOT_PASSWORD: bry190809
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:

